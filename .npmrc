# @base-ui/react deklaruje date-fns@^4 jako peerOptional, a projekt używa v3.
# Zależność jest opcjonalna i nieużywana przez Base UI w tym projekcie, ale npm
# i tak przerywa instalację. Bez tej flagi `npm ci` po sklonowaniu nie przechodzi.
legacy-peer-deps=true
